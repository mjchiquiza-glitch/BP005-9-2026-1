#include <stdio.h>
   int main() {
     int x, y;
   printf("Ingrese el primer digito: ");
   scanf("%d", &x);

   printf("Ingrese el segundo digito: ");
   scanf("%d", &y);
     if (x > y) {
   printf("%d es mayor que %d\n", x, y);
  }
return 0;
}
