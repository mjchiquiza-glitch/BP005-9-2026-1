#ifndef TABLERO_H
#define TABLERO_H

// Definimos el tamaño del tablero
const int FILAS = 15;
const int COLUMNAS = 20;

// Funciones obligatorias para el tablero
void limpiarTablero(char tablero[FILAS][COLUMNAS]);
void mostrarTablero(char tablero[FILAS][COLUMNAS]);

#endif
